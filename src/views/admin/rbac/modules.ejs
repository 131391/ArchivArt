<script>
// Make formatter functions available globally for table formatting
window.formatIcon = function(item) {
    const iconPath = item.icon || item;
    if (!iconPath) return '<span class="text-gray-400 italic">No icon</span>';
    return `<div class="flex items-center space-x-2">
        <i class="${iconPath} text-indigo-600 text-sm"></i>
        <span class="text-xs text-gray-500 font-mono">${iconPath}</span>
    </div>`;
};

window.formatRoute = function(item) {
    const route = item.route || item;
    if (!route) return '<span class="text-gray-400 italic">No route</span>';
    return `<div class="flex items-center space-x-2">
        <i class="fas fa-link text-blue-600 text-xs"></i>
        <span class="text-sm font-mono text-gray-700">${route}</span>
    </div>`;
};

window.formatOrderIndex = function(item) {
    const orderIndex = item.order_index !== undefined ? item.order_index : item;
    if (orderIndex === null || orderIndex === undefined) return '<span class="text-gray-400 italic">No order</span>';
    const orderText = orderIndex === 0 ? 'First' : 
                     orderIndex === 1 ? 'Second' : 
                     orderIndex === 2 ? 'Third' : 
                     orderIndex === 3 ? 'Fourth' : 
                     orderIndex === 4 ? 'Fifth' : 
                     `Position ${orderIndex}`;
    return `<div class="flex items-center space-x-2">
        <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-semibold bg-blue-100 text-blue-800">
            <i class="fas fa-sort-numeric-up mr-1"></i>
            ${orderText}
        </span>
        <span class="text-xs text-gray-500">(${orderIndex})</span>
    </div>`;
};

window.formatActionCount = function(item) {
    const count = item.action_count !== undefined ? item.action_count : item;
    if (!count || count === 0) return '<span class="text-gray-400 italic">No actions</span>';
    const actionText = count === 1 ? '1 Action' : `${count} Actions`;
    return `<div class="flex items-center space-x-2">
        <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-semibold bg-green-100 text-green-800">
            <i class="fas fa-cogs mr-1"></i>
            ${actionText}
        </span>
    </div>`;
};

window.formatPermissionCount = function(item) {
    const count = item.permission_count !== undefined ? item.permission_count : item;
    if (!count || count === 0) return '<span class="text-gray-400 italic">No permissions</span>';
    const permissionText = count === 1 ? '1 Permission' : `${count} Permissions`;
    return `<div class="flex items-center space-x-2">
        <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-semibold bg-purple-100 text-purple-800">
            <i class="fas fa-key mr-1"></i>
            ${permissionText}
        </span>
    </div>`;
};

// Also create local function references for backward compatibility
function formatIcon(item) { return window.formatIcon(item); }
function formatRoute(item) { return window.formatRoute(item); }
function formatOrderIndex(item) { return window.formatOrderIndex(item); }
function formatActionCount(item) { return window.formatActionCount(item); }
function formatPermissionCount(item) { return window.formatPermissionCount(item); }
</script>

<!-- Include Common Table Component -->
<%- include('../../components/table', {
    title: title,
    data: data,
    showSearch: true,
    showFilters: false,
    searchPlaceholder: 'Search modules...',
    columns: [
        { key: 'id', label: 'ID', sortable: true },
        { key: 'display_name', label: 'Module', sortable: true },
        { key: 'name', label: 'Name', sortable: true },
        { key: 'icon', label: 'Icon', sortable: false, type: 'custom', formatter: 'formatIcon' },
        { key: 'route', label: 'Route', sortable: true, type: 'custom', formatter: 'formatRoute' },
        { key: 'order_index', label: 'Order', sortable: true, type: 'custom', formatter: 'formatOrderIndex' },
        { key: 'action_count', label: 'Actions', sortable: true, type: 'custom', formatter: 'formatActionCount' },
        { key: 'permission_count', label: 'Permissions', sortable: true, type: 'custom', formatter: 'formatPermissionCount' }
    ],
    actions: [
        { name: 'view', label: 'View', icon: 'fas fa-eye', class: 'text-blue-600 hover:text-blue-900', onclick: (item) => `viewModule(${item.id})` },
        { name: 'edit', label: 'Edit', icon: 'fas fa-edit', class: 'text-indigo-600 hover:text-indigo-900', onclick: (item) => `editModule(${item.id})` },
        { name: 'actions', label: 'Actions', icon: 'fas fa-cogs', class: 'text-green-600 hover:text-green-900', onclick: (item) => `manageModuleActions(${item.id})` },
        { name: 'delete', label: 'Delete', icon: 'fas fa-trash', class: 'text-red-600 hover:text-red-900', onclick: (item) => `deleteModule(${item.id})` }
    ],
    pagination: pagination,
    search: search,
    filters: filters,
    showSearch: true,
    searchPlaceholder: 'Search modules...',
    emptyIcon: 'fas fa-puzzle-piece',
    emptyTitle: 'No Modules Found',
    emptyMessage: 'There are no modules to display.',
    emptySearchMessage: 'No modules match your search criteria.',
    headerButtons: [
        { text: 'Create Module', href: '#', icon: 'fas fa-plus', onclick: 'createModule()' }
    ]
}) %>

<!-- Enhanced Module View Modal -->
<div id="viewModuleModal" class="fixed inset-0 bg-black bg-opacity-60 overflow-y-auto h-full w-full hidden z-40 backdrop-blur-sm">
    <div class="relative top-20 mx-auto p-0 border-0 max-w-lg w-full mx-4 shadow-2xl rounded-2xl bg-white transform transition-all duration-300 ease-out scale-95 opacity-0" id="viewModuleModalContainer">
        <!-- Modal Header with Gradient -->
        <div class="bg-gradient-to-r from-purple-500 to-pink-600 rounded-t-2xl p-6 text-white">
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <div class="flex-shrink-0 mr-4">
                        <i class="fas fa-puzzle-piece text-white text-3xl"></i>
                    </div>
                    <h3 class="text-xl font-bold">Module Details</h3>
                </div>
                <button onclick="closeViewModal()" class="text-white hover:text-gray-200 transition-colors duration-200 p-2 rounded-full hover:bg-white hover:bg-opacity-20">
                    <i class="fas fa-times text-lg"></i>
                </button>
            </div>
        </div>
        
        <!-- Modal Body -->
        <div class="p-6">
            <div id="moduleDetails">
                <!-- Module details will be loaded here -->
            </div>
        </div>
    </div>
</div>

<!-- Enhanced Module Edit Modal -->
<div id="editModuleModal" class="fixed inset-0 bg-black bg-opacity-60 overflow-y-auto h-full w-full hidden z-40 backdrop-blur-sm">
    <div class="relative top-20 mx-auto p-0 border-0 max-w-lg w-full mx-4 shadow-2xl rounded-2xl bg-white transform transition-all duration-300 ease-out scale-95 opacity-0" id="editModuleModalContainer">
        <!-- Modal Header with Gradient -->
        <div class="bg-gradient-to-r from-blue-500 to-indigo-600 rounded-t-2xl p-6 text-white">
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <div class="flex-shrink-0 mr-4">
                        <i class="fas fa-puzzle-piece text-white text-3xl"></i>
                    </div>
                    <h3 class="text-xl font-bold">Edit Module</h3>
                </div>
                <button onclick="closeEditModal()" class="text-white hover:text-gray-200 transition-colors duration-200 p-2 rounded-full hover:bg-white hover:bg-opacity-20">
                    <i class="fas fa-times text-lg"></i>
                </button>
            </div>
        </div>
        
        <!-- Modal Body -->
        <div class="p-6">
            <form id="editModuleForm">
                <input type="hidden" id="editModuleId" name="id">
                
                <div class="mb-6">
                    <label for="editModuleName" class="block text-sm font-semibold text-gray-700 mb-2">Module Name</label>
                    <input type="text" id="editModuleName" name="name" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <div class="mb-6">
                    <label for="editModuleDisplayName" class="block text-sm font-semibold text-gray-700 mb-2">Display Name</label>
                    <input type="text" id="editModuleDisplayName" name="display_name" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <div class="mb-6">
                    <label for="editModuleDescription" class="block text-sm font-semibold text-gray-700 mb-2">Description</label>
                    <textarea id="editModuleDescription" name="description" rows="3" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white"></textarea>
                </div>
                
                <div class="mb-6">
                    <label for="editModuleIcon" class="block text-sm font-semibold text-gray-700 mb-2">Icon (FontAwesome class)</label>
                    <input type="text" id="editModuleIcon" name="icon" placeholder="fas fa-puzzle-piece" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <div class="mb-6">
                    <label for="editModuleRoute" class="block text-sm font-semibold text-gray-700 mb-2">Route</label>
                    <input type="text" id="editModuleRoute" name="route" placeholder="/admin/module" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <div class="mb-6">
                    <label for="editModuleOrder" class="block text-sm font-semibold text-gray-700 mb-2">Order Index</label>
                    <input type="number" id="editModuleOrder" name="order_index" min="0" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <!-- Modal Actions -->
                <div class="flex justify-end space-x-4">
                    <button type="button" onclick="closeEditModal()" class="px-6 py-3 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-gray-300 transition-all duration-200 font-medium">
                        Cancel
                    </button>
                    <button type="submit" class="px-6 py-3 bg-gradient-to-r from-blue-500 to-indigo-600 text-white rounded-xl hover:from-blue-600 hover:to-indigo-700 focus:outline-none focus:ring-2 focus:ring-blue-300 transition-all duration-200 font-medium shadow-lg hover:shadow-xl transform hover:scale-105">
                        <i class="fas fa-save mr-2"></i>
                        Update Module
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Enhanced Create Module Modal -->
<div id="createModuleModal" class="fixed inset-0 bg-black bg-opacity-60 overflow-y-auto h-full w-full hidden z-40 backdrop-blur-sm">
    <div class="relative top-20 mx-auto p-0 border-0 max-w-lg w-full mx-4 shadow-2xl rounded-2xl bg-white transform transition-all duration-300 ease-out scale-95 opacity-0" id="createModuleModalContainer">
        <!-- Modal Header with Gradient -->
        <div class="bg-gradient-to-r from-green-500 to-emerald-600 rounded-t-2xl p-6 text-white">
            <div class="flex items-center justify-between">
                <div class="flex items-center">
                    <div class="flex-shrink-0 mr-4">
                        <i class="fas fa-plus text-white text-3xl"></i>
                    </div>
                    <h3 class="text-xl font-bold">Create Module</h3>
                </div>
                <button onclick="closeCreateModal()" class="text-white hover:text-gray-200 transition-colors duration-200 p-2 rounded-full hover:bg-white hover:bg-opacity-20">
                    <i class="fas fa-times text-lg"></i>
                </button>
            </div>
        </div>
        
        <!-- Modal Body -->
        <div class="p-6">
            <form id="createModuleForm">
                <div class="mb-6">
                    <label for="createModuleName" class="block text-sm font-semibold text-gray-700 mb-2">Module Name</label>
                    <input type="text" id="createModuleName" name="name" required class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <div class="mb-6">
                    <label for="createModuleDisplayName" class="block text-sm font-semibold text-gray-700 mb-2">Display Name</label>
                    <input type="text" id="createModuleDisplayName" name="display_name" required class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <div class="mb-6">
                    <label for="createModuleDescription" class="block text-sm font-semibold text-gray-700 mb-2">Description</label>
                    <textarea id="createModuleDescription" name="description" rows="3" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white"></textarea>
                </div>
                
                <div class="mb-6">
                    <label for="createModuleIcon" class="block text-sm font-semibold text-gray-700 mb-2">Icon (FontAwesome class)</label>
                    <input type="text" id="createModuleIcon" name="icon" placeholder="fas fa-puzzle-piece" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <div class="mb-6">
                    <label for="createModuleRoute" class="block text-sm font-semibold text-gray-700 mb-2">Route</label>
                    <input type="text" id="createModuleRoute" name="route" placeholder="/admin/module" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <div class="mb-6">
                    <label for="createModuleOrder" class="block text-sm font-semibold text-gray-700 mb-2">Order Index</label>
                    <input type="number" id="createModuleOrder" name="order_index" min="0" value="0" class="w-full px-4 py-3 border border-gray-300 rounded-xl shadow-sm focus:outline-none focus:ring-2 focus:ring-green-500 focus:border-transparent transition-all duration-200 bg-gray-50 focus:bg-white">
                </div>
                
                <!-- Modal Actions -->
                <div class="flex justify-end space-x-4">
                    <button type="button" onclick="closeCreateModal()" class="px-6 py-3 bg-gray-100 text-gray-700 rounded-xl hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-gray-300 transition-all duration-200 font-medium">
                        Cancel
                    </button>
                    <button type="submit" class="px-6 py-3 bg-gradient-to-r from-green-500 to-emerald-600 text-white rounded-xl hover:from-green-600 hover:to-emerald-700 focus:outline-none focus:ring-2 focus:ring-green-300 transition-all duration-200 font-medium shadow-lg hover:shadow-xl transform hover:scale-105">
                        <i class="fas fa-plus mr-2"></i>
                        Create Module
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>
